# Install ../../sdk as a packed copy, not a symlink. A symlinked SDK resolves
# @polkadot/* from sdk/node_modules while this package resolves its own copy,
# and polkadot-js then warns (correctly) about two instances of the same crypto.
install-links=true
