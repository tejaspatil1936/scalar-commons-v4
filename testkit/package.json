{
  "name": "@scalar-commons/testkit",
  "version": "0.0.0",
  "private": true,
  "description": "Chain lifecycle integration tests. Run against a local dev node (SCALAR_WS, default ws://127.0.0.1:9955). Never point at the public testnet.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@polkadot/api": "^15.0.0",
    "@polkadot/keyring": "^13.0.0",
    "@polkadot/util-crypto": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "engines": { "node": ">=18" }
}
